env: none
configSource: "WMCS-App-1-Base"
serilog:
  logging:
    level: debug
management:
  endpoints:
    actuator:
      exposure:
        include: ["*"]
    health:
      showdetails: always
      enabled: true
ASPNETCORE_ENVIRONMENT: "Development"
