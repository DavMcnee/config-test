env: none
configSource: "WMCS-App-1-Base (feature 1)"
serilog:
  logging:
    level: debug
management:
  endpoints:
    actuator:
      exposure:
        include: ["*"]
    health:
      showdetails: always
      enabled: true
ASPNETCORE_ENVIRONMENT: "Development"
features:
  enabled: 
    - feature_1
    - feature_2
  disabled:
    - features_99
